# Validate inputs for spread_weighted

Validate inputs for spread_weighted

## Usage

``` r
.chck_spread_weighted(values, weights, value_cols = NULL)
```

## Arguments

- values:

  Numeric vector, matrix or data.frame of values

- weights:

  Multi-layer SpatRaster of weights

- value_cols:

  Column names if values is data.frame
