# Collapse a field by retaining selected axes

\`by\` names the axes to keep. Every other axis is summed away. \`by =
NULL\` returns a scalar total.

## Usage

``` r
.ae_aggregate(field, by = NULL)
```
