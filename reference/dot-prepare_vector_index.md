# Prepare a vector index frame and re-key vector values

Prepare a vector index frame and re-key vector values

## Usage

``` r
.prepare_vector_index(
  data,
  domain,
  frame = NULL,
  allow_positional = FALSE,
  meta = list()
)
```
