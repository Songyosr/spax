# Low-level vector spax_field constructor

Low-level vector spax_field constructor

## Usage

``` r
.new_spax_vector_field(
  data,
  domain,
  index = NULL,
  role = "unknown",
  meta = list()
)
```
