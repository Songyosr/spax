# Resolve a raster layer frame from raw input conventions

Resolve a raster layer frame from raw input conventions

## Usage

``` r
.resolve_raster_layer_frame(
  data,
  domain,
  frame = NULL,
  allow_positional = FALSE
)
```
