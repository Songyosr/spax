# Convert a weight/kernel field into a mapping field

\`by\` names the axes retained while every other axis competes. The
output keeps the input field's domain/backend/index and uses role "map".

## Usage

``` r
.ae_normalize(field, by = NULL, method = "standard", a0 = 0)
```
