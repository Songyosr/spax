# Coerce supply values to one vector field per measure

Coerce supply values to one vector field per measure

## Usage

``` r
.fca_supply_fields(
  supply,
  kernel,
  id_col = NULL,
  supply_cols = NULL,
  indicator_names = NULL
)
```
