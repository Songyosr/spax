# Normalize a resolved vector node frame and attach provenance

Normalize a resolved vector node frame and attach provenance

## Usage

``` r
.normalize_vector_node_frame(data, domain, resolved, meta = list())
```
