# Core computation for demand transformation

Core computation for demand transformation

## Usage

``` r
.help_transform_demand_core(
  x,
  value_col,
  pmf = NULL,
  template = NULL,
  density = FALSE
)
```
