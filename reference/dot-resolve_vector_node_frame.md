# Resolve a vector node frame from raw input conventions

Resolve a vector node frame from raw input conventions

## Usage

``` r
.resolve_vector_node_frame(
  data,
  domain,
  frame = NULL,
  allow_positional = FALSE
)
```
