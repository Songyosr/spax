# Validate inputs for gather_weighted

Validate inputs for gather_weighted

## Usage

``` r
.chck_gather_weighted(values, weights, na.rm)
```

## Arguments

- values:

  SpatRaster of values

- weights:

  SpatRaster of weights

- na.rm:

  Logical for NA handling
