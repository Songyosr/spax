# Reorder a vector field's values to a raster field's layer order

Reorder a vector field's values to a raster field's layer order

## Usage

``` r
.ae_align_to_layers(vector_field, raster_field)
```
