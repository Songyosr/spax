# Create a vector spax_field

Create a vector spax_field

## Usage

``` r
.spax_vector_field(
  data,
  domain,
  index = NULL,
  frame = NULL,
  role = "unknown",
  meta = list(),
  allow_positional = FALSE,
  snap = FALSE
)
```
