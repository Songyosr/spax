# Normalize raster fields when the retained axes do not include the cell axis

Normalize raster fields when the retained axes do not include the cell
axis

## Usage

``` r
.ae_normalize_raster_by_global(field, by, method, a0 = 0)
```
