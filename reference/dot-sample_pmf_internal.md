# Internal sampling function (moved from main body)

Internal sampling function (moved from main body)

## Usage

``` r
.sample_pmf_internal(
  x,
  n,
  size,
  prob,
  method,
  iterations,
  evolve_prop,
  replace_0,
  snap,
  ...
)
```
