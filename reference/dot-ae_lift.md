# Lift a field onto a template field's structure

The template supplies backend, domain, geometry, and index frame. Its
values are ignored. Source values are matched on source axes and
broadcast over axes present only in the template.

## Usage

``` r
.ae_lift(field, template)
```
