# Align vector values to a target frame by the vector field's full domain

Align vector values to a target frame by the vector field's full domain

## Usage

``` r
.ae_align_vector_to_frame(vector_field, target_frame)
```
