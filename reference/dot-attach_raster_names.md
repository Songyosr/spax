# Helper function to attach names to raster if available

Helper function to attach names to raster if available

## Usage

``` r
.attach_raster_names(rast, base_path)
```

## Arguments

- rast:

  SpatRaster object

- base_path:

  Path to the raster file

## Value

SpatRaster with names attached if available
