# Build a raster field from already-consistent data + frame (snap construction)

Build a raster field from already-consistent data + frame (snap
construction)

## Usage

``` r
.ae_raster(data, domain, frame, role = "unknown", meta = list())
```
