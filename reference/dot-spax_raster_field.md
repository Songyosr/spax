# Create a raster spax_field

Create a raster spax_field

## Usage

``` r
.spax_raster_field(
  data,
  domain,
  index = NULL,
  frame = NULL,
  role = "unknown",
  meta = list(),
  allow_positional = FALSE,
  snap = FALSE
)
```
