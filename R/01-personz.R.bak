# R/01-personz.R
create_personz <- function(name, age) {
  structure(
    list(
      name = name,
      age = age
    ),
    class = c("personz", "list")
  )
}

# print.personz <- function(x, ...) {
#   cat("Person object:\n")
#   cat("  Name:", x$name, "\n")
#   cat("  Age:", x$age, "\n")
#   invisible(x)
# }
