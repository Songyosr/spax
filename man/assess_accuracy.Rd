% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01-iterative_chain_aff.R, R/02_assess_ifca.R
\name{assess_accuracy}
\alias{assess_accuracy}
\title{Assess prediction accuracy of iFCA model}
\usage{
assess_accuracy(predicted, observed, names = NULL)

assess_accuracy(predicted, observed, names = NULL)
}
\arguments{
\item{predicted}{Numeric vector of model-predicted utilization values}

\item{observed}{Numeric vector of observed utilization values}

\item{names}{Optional character vector of facility names}
}
\value{
List containing:
  \describe{
    \item{summary}{Data frame of facility-specific prediction errors}
    \item{metrics}{List of overall accuracy metrics (MAE, RMSE, MAPE)}
    \item{errors}{Detailed error components for further analysis}
  }

List containing:
  \describe{
    \item{summary}{Data frame of facility-specific prediction errors}
    \item{metrics}{List of overall accuracy metrics (MAE, RMSE, MAPE)}
    \item{errors}{Detailed error components for further analysis}
  }
}
\description{
Evaluates the accuracy of an iterative floating catchment area model by
comparing predicted utilization against observed values. Provides both
overall accuracy metrics and facility-specific error analysis.

Evaluates the accuracy of an iterative floating catchment area model by
comparing predicted utilization against observed values. Provides both
overall accuracy metrics and facility-specific error analysis.
}
\examples{
# Create simulated observed data
observed_values <- hc12_hos$d_pop_moph_05 * 0.1  # 10\% visit rate

# Run iFCA model
distance <- terra::rast(hos_iscr)
pop <- terra::rast(u5pd)
model <- spax_ifca(
  distance_raster = distance,
  demand = pop,
  supply = hc12_hos$s_doc,
  sigma = 30
)

# Compare with observed data
accuracy <- assess_accuracy(
  predicted = model$utilization,
  observed = observed_values
)

# Examine results
print(accuracy$metrics)
\dontrun{
# Run iFCA model
model <- spax_ifca(distance_raster, demand, supply)

# Compare with observed data
accuracy <- assess_accuracy(
  predicted = model$utilization,
  observed = observed_values
)

# Examine results
print(accuracy$metrics)
plot(accuracy)
}
}
