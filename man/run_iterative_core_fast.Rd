% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iterative_chain.R
\name{run_iterative_core_fast}
\alias{run_iterative_core_fast}
\title{#' Update facility names while preserving order
#' @param names Character vector of facility names
#' @param zero_map Logical vector marking zero-supply facilities
#' @return List with name mapping information
#' @keywords internal
update_facility_names <- function(names, zero_map) {
  valid_names <- names[!zero_map]
  list(
    original = names,
    valid = valid_names,
    mapping = match(valid_names, names)
  )
}}
\usage{
run_iterative_core_fast(
  supply,
  weights,
  demand,
  lambda = 0.5,
  max_iter = 100,
  tolerance = 1e-06,
  window_size = 5
)
}
\description{
#' Update facility names while preserving order
#' @param names Character vector of facility names
#' @param zero_map Logical vector marking zero-supply facilities
#' @return List with name mapping information
#' @keywords internal
update_facility_names <- function(names, zero_map) {
  valid_names <- names[!zero_map]
  list(
    original = names,
    valid = valid_names,
    mapping = match(valid_names, names)
  )
}
}
